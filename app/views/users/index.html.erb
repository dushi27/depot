<%= link_to 'Add New User', new_user_path, class:'btn btn-lg btn-info pull-right' %>
<h1>Current Users</h1><br>
<p id="notice"><%= notice %></p>

<table class='table'>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
  </tr>
  <% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= link_to 'Actions', edit_user_path(user) %></td>
  </tr>
  
  <% end %>
</table><br />
